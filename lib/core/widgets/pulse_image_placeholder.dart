import 'package:flutter/material.dart';

class PulseImagePlaceholder extends StatelessWidget {
  const PulseImagePlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset("assets/images/pulse_animation.gif",);
  }
}
